export const getStackTabTabId = (tabId: string): string => `stack-tab-tab-${encodeURIComponent(tabId)}`

export const getStackTabPanelId = (tabId: string): string => `stack-tab-panel-${encodeURIComponent(tabId)}`
